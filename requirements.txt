# Scanner (the gate path) needs only PyYAML; everything below is server/tooling.
pyyaml>=6.0

# Evidence server
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# Reports
reportlab>=4.2

# Tests
pytest>=8.0
jsonschema>=4.20
httpx>=0.27

# Evidence signing (optional). dilithium-py is a DEVELOPMENT backend: pure Python,
# unaudited, and refused by the FIPS profiles. Prefer liboqs where the native library
# can be installed out of band.
dilithium-py>=1.1
